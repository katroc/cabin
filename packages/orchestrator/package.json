{
  "name": "@rag-assistant/orchestrator",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/**/*.ts",
    "test": "jest"
  },
  "dependencies": {
    "@rag-assistant/core": "workspace:*",
    "@rag-assistant/retrieval": "workspace:*",
    "@rag-assistant/generation": "workspace:*",
    "@rag-assistant/data-ingestion": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0"
  }
}